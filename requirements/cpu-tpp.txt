# Common dependencies
-r requirements-common.txt

# Dependencies for CPUs
torch==2.6.0+cpu; platform_machine != "ppc64le" and platform_machine != "aarch64"
torch==2.6.0; platform_machine == "aarch64"
torchvision==0.19.0+cpu; platform_machine != "ppc64le"  # required for the image processor of phi3v, this must be updated alongside torch
datasets # for benchmark scripts
ninja 
setuptools 
tqdm 
future 
cmake 
numpy 
pyyaml 
scikit-learn 
pydot
torchaudio
pybind11
h5py
onnx
tensorboardx
black==22.6.0
clang-format==6.0.1
intel-openmp
