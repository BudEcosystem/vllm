# Common dependencies
-r common.txt

# Dependencies for CPUs
torch==2.7.0+cpu; platform_machine != "ppc64le" and platform_machine != "aarch64"
torch==2.7.0; platform_machine == "aarch64"
torchvision==0.22.0+cpu; platform_machine != "ppc64le"  # required for the image processor of phi3v, this must be updated alongside torch
--extra-index-url https://pytorch-extension.intel.com/release-whl/stable/cpu/us/
oneccl_bind_pt==2.7.0
datasets # for benchmark scripts
ninja 
setuptools 
tqdm 
future 
cmake 
numpy 
pyyaml 
scikit-learn 
pydot
torchaudio
pybind11
h5py
onnx
tensorboardx
black==22.6.0
clang-format==6.0.1
intel-openmp
